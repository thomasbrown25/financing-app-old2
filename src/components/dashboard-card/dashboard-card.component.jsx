import {
    CardBody,
    DashboardCardContainer,
    CardHeader
} from './dashboard-card.styles';

const DashboardCard = ({ header, body }) => {
    return (
        <DashboardCardContainer>
            <CardBody>
                <div></div>
            </CardBody>
        </DashboardCardContainer>
    );
};

export default Card;
